import React from 'react';
import MainPage from './components/MainPage'; // MainPage 컴포넌트를 import합니다.

const App = () => {
    return (
        <div className="App">
            <MainPage />  {/* MainPage 컴포넌트를 렌더링합니다. */}
        </div>
    );
};

export default App;
